/* 产生真正1像素高度的 mixin */
border-1px($color)
  position: relative
  &:after
    display block
    position absolute
    left 0
    bottom 0
    width 100%
    border-top solid 1px $color
    content ' '
